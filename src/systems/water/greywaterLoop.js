/**
 * greywaterLoop.js
 * Closed-loop water: potable draw -> greywater -> reclamation -> potable, with
 * losses at each stage made up by groundwater intake. Reclamation capacity is
 * the binding constraint on population growth.
 */

export function tick(state) {
  // TODO: draw, contaminate, reclaim, account for losses and intake
}

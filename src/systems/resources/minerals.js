/**
 * minerals.js
 * Raw extraction: ore bodies, seam depletion, yield per worker-shift, and the
 * quality grades that feed the component chain.
 */

export function tick(state) {
  // TODO: for each active mine, extract against remaining seam
}

export function depleteSeam(seam, extracted) {
  // TODO
}

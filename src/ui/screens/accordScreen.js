/**
 * accordScreen.js
 * Reading and amending the Accord. Shows article text, version history, which
 * systems each article currently affects, and any detected conflicts.
 */
export function mount(root, state) { /* TODO */ }
export function update(state) { /* TODO */ }

/**
 * chapter2/chairLogic.js
 * The player is the Chair: the hidden authority behind a public Mayor they do
 * not directly control. Two ledgers now — what the settlement believes and
 * what is true — and the gap between them is the resource being managed.
 */

export function setup(state, config) {
  // TODO: carry-over from Chapter 1, seed the mole, open the Apocrypha
}

export function tick(state) {
  // TODO
}

/** The public Mayor acts semi-autonomously; the Chair steers, not commands. */
export function proxyMayorAct(state) {
  // TODO
}

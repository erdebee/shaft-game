/**
 * chapter2/presidiumCoup.js
 * Chapter 2's climax. The player's move against the Presidium fails by design,
 * but how it fails should reflect the run.
 *
 * The demonstration is infrastructural, not military: supply of the scrubber
 * catalyst stops. The settlement has never produced it and cannot start. Air
 * degrades from the deep levels upward until the Board capitulates.
 *
 * Resolution: the pre-planted mole is installed as the new public leader, and
 * the player is left holding an authority that was never theirs.
 */

export function arm(state) {
  // TODO: preconditions that make the coup available
}

export function execute(state) {
  // TODO: cut catalyst supply, run the degradation clock, force capitulation
}

export function resolve(state) {
  // TODO: install the mole, carry state forward to the epilogue
}
